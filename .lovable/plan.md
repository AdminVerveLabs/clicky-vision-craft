

## Update Social Credibility Banner Background

**File: `src/pages/ClassesPage.tsx`**

Change the banner's `bg-cream` class to a custom light green background using an inline style or Tailwind class that matches the green CTA color (`hsl(105, 48%, 46%)`) at ~8-10% opacity, e.g. `bg-[hsla(105,48%,46%,0.08)]`.

